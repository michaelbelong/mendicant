import {listReactFiles} from 'list-react-files'

const randomWorkDirectory = "./assets"

listReactFiles(randomWorkDirectory).then(files => console.log(files))

const RandomWork = [
	'',
	''
]

export default RandomWork